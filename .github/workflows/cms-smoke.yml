name: CMS smoke

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements.txt
      - name: Compile backend
        run: python -m compileall -q backend
      - name: Import application
        run: python healthcheck.py
      - name: Start CMS
        env:
          SECRET_KEY: ci-smoke-secret
          APP_ENV: development
          PORT: '8000'
        run: |
          nohup python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000 > /tmp/cms.log 2>&1 &
          echo $! > /tmp/cms.pid
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:8000/health; then break; fi
            sleep 2
          done
          curl -fsS http://127.0.0.1:8000/health
      - name: Check readiness
        run: curl -fsS http://127.0.0.1:8000/ready
      - name: Stop CMS
        if: always()
        run: |
          if [ -f /tmp/cms.pid ]; then kill $(cat /tmp/cms.pid) || true; fi
          cat /tmp/cms.log || true

# CMS-my — Отчёт о проделанной работе (2026-08-27)

## Статус
- Сайт запущен и работает. Проверка:
  - `/health` → 200 OK
  - `/ready` → 200 OK
  - Все 12 страниц с авторизацией → 200 OK
  - Все API endpoints с авторизацией → 200 OK
- Файлы сохранены в ветку `jeevgenij-coder-cms` репозитория Cms-my.

## Что сделано (проверено и работает)

### 1. Risk Score (0–100, лимит 60)
- Файл: `backend/risk_management.py`
- Добавлены `calculate_risk_score()` и `check_risk_score()`, константа `MAX_RISK_SCORE = 60`
- API: `POST /api/risk/score` (проверено: leverage=1.5 → score 31 → разрешено; риск 71 → заблокирован)
- API: `/api/risk/status` → возвращает `current_risk_score` и `max_risk_score`
- Вставлены проверки в `bot_start` (возвращает risk_score=38), `execute_strategy`, `arbitrage_scan`
- UI: Risk Score отображается, playbook работает

### 2. Арбитражный движок (Arbitrage Engine)
- Файл: `backend/modules/arbitrage_engine.py` (новая, ~330 строк)
- Стратегии: Triangular, Cross-Exchange, Statistical, Funding-Rate + главный движок
- Страница: `templates/arbitrage.html`
- API: `/api/arbitrage/start`, `/stop`, `/status`, `/scan`
- Маршрут `GET/POST /arbitrage` в `backend/main.py`
- Ссылка добавлена в сайдбар `templates/base.html`
- Для арбитража между биржами: отдельная кнопка и настройки второго API

### 3. Второй API ключ для арбитража
- Поля в Wallet: `exchange_provider_arb`, `exchange_key_masked_arb`, `exchange_sandbox_arb`
- Миграция БД выполнена (ALTER TABLE в `cms_core.db`)
- API: `POST /api/user/connect-arbitrage-exchange`
- UI: форма подключения второй биржи в `templates/arbitrage.html`

### 4. Сигналы для ручной торговли
- `templates/manual_trading.html` — блок «Настройки сигналов» (источник: бот/ИИ/ИИ+бот, порог %)
- Кнопка «Apply Signal» вызывает `/api/market/signal`

### 5. Настройки сигналов в боте
- `templates/bot_management.html` — блок «Настройки сигналов» (источник, порог уверенности, auto-apply)

### 6. Info-тултипы (краткое описание функций по значку инфо)
- `templates/base.html` — система info-тултипов (CSS + JS + словарь CMS_INFO)
- data-info добавлены ко всем ссылкам навигации

### 7. Темы и дизайн (ранее)
- Тёмная / светлая / автоматическая темы
- Pionex-стиль, адаптивный под мобильные устройства
- Профиль → настройки подключения

### 8. Ранняя работа (проверено ранее)
- Бот автоматически создаёт, тестирует и публикует стратегии в маркет
- Демо-режим на 100€ (live-данные, без знания будущего)
- 10 стратегий, динамические параметры, создание стратегий
- Копи-трейдинг, маркетплейс platinum/premium
- Разделение: тесты стратегий + демо
- Live-график со свечами

## Что нужно сделать (TODO)

### Бот и стратегии
- [ ] При выборе стратегии показывать все параметры (риск, стоп-лосс %, диапазон, авто)
- [ ] Параметры должны меняться при выборе стратегии
- [ ] Бот должен следовать выбранной стратегии даже в auto mode
- [ ] Full auto mode — разрешить боту менять стратегии / торговать самостоятельно (память, обучение, советы ИИ)
- [ ] Демо-режим: убедиться, что бот совершает сделки (сейчас ни одной не сделал в демо)
- [ ] Страницы бота: расширить функционал или отдельная страница бота со своими функциями

### Арбитраж
- [ ] Сигнал для арбитража + настройки «применить»
- [ ] Убедиться, что арбитражный бот может продавать/покупать среди подключённых бирж
- [ ] Выбор стратегии арбитража (Triangular / Cross-Exchange / Statistical / Funding)
- [ ] Интеграция с демо, тестами и реальной торговлей

### Сигналы
- [ ] Настройки выдаваемого сигнала (от бота / ИИ / ИИ+бот)
- [ ] Применение сигнала в ручной торговле и арбитраже
- [ ] Индикация сигнала ИИ для ручной торговли + «применить»

### Общее
- [ ] Полная проверка всех модулей/плагинов/стратегий (работают с ботом, тестами, демо)
- [ ] Работоспособность маркета (платины, модули, стратегии)
- [ ] Демо должно быть идентично реальной торговле (функции, интерфейс), но на 100€
- [ ] Работоспособность маркета за CMSC
- [ ] (Из-за 403 push на GitHub) — залить ветку `jeevgenij-coder-cms` на `bosszeka40-pixel/Cms-my` (нужен токен/доступ для `jevvgenij-coder`)

## Важно (не менять)
- Не менять логику `backend/bot.py` и `backend/hft_brain.py` — задуманную логику Bot + AI менять нельзя
- Можно только добавлять функции
- Всё работает через API подключённых бирж
- Демо и тесты — на реальных данных (live) без знания будущего до завершения сделки
